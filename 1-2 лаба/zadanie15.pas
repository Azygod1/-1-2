program KirillP;
var a,b,c,i:integer;
begin
  writeln('Введите три числа');
  read(a,b,c);
  i:=0;
  if a<0 then 
  else inc(i);
    if b<0 then 
  else inc(i);
    if c<0 then 
  else inc(i);
    writeln('Количество положительных чисел = ',i)
end.